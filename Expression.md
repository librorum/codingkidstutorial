# 표현식(Expression) 카테고리들

* [액터](#actor)
* [조건](#condition)
* [변수](#variable)
* [수학](#math)
* [물리](#physics)
* [랜덤](#random)
* [터치](#touch)
* [데이터](#data)
* [문자열](#string)
* [날짜시간](#datetime)
* [기타](#etc)
* [하드웨어](#hardware)

---

# 액터 {#actor}
* 이 액터의 이름
* 액터[자신]의 글자
* 액터[자신]의 x좌표
* 액터[자신]의 y좌표
* 액터[자신]의 현재 회전각
* 액터[자신]의 너비
* 액터[자신]의 높이
* 액터[자신]의 배율
* 액터[자신]의 가로 배율
* 액터[자신]의 세로 배율
* 충돌한 액터
* 충돌한 각도
* [자신]이 복사된 액터라면
* 액터[자신]이 화면밖으로 나가면
* 복사 순서 번호
* 액터[자신]이 복사한 횟수
* 액터[자신]이 숨겨졌다면
* 액터[자신]이 애니메이션 중이라면
* 액터[자신]의 태그
* 액터[자신]의 현재 그림 인덱스
* 액터[자신]이 가진 이미지 갯수
* 태그[]를 가진 액터
* 총 액터 갯수
* 액터[]가 액터[]를 자식으로 가지고 있다면
* 얼굴의 x좌표
* 얼굴의 y좌표
* 왼쪽 눈 x좌표
* 왼쪽 눈 y좌표
* 오른쪽 눈 x좌표
* 오른쪽 눈 y좌표


---

# 조건 {#condition}
* [] = []
* [] ≠ []
* [] < []
* [] > []
* [] <= []
* [] >= []
* [] 그리고 []
* [] 또는 []
* []의 반대

---

# 변수 {#variable}
* 변수[]
* 함수 리턴값
----

# 수학 {#math}
* [] + []
* [] - []
* [] * []
* [] / []
* [] 나누기 []의 나머지(모듈라)
* [] 을 []의 나머지로 나눈 몫
* [] <= [] <= []
* [] <= [] < []
* [] < [] <= []
* [] < [] < []
* 액터[]로 부터 액터[]까지의 거리
* 액터[]로 부터 액터[]까지의 각도
* 액터[]로 부터 좌표(x[], y[])  까지의 거리
* 액터[]로 부터 좌표x(x[], y[]) 까지의 각도
* sin([])
* cos([])
* tan([])
* asin([])
* acos([])
* atan([])
* atan2([], [])
* π(PI)
* 절대값 abs([])
* 반올림 round([])
* 정수값 int([])
* []의 자릿수[1] 고정소수점 값 
* 제곱근 sqrt([])
* 거듭제곱 pow[10]^[2]
* 자연로그 ln[2.71828]
* 상용로그 log(100)
* []의 해쉬값, 방식[]
* e(자연상수)

---

# 물리 {#physics}
* 액터[자신]의 x축 속도
* 액터[자신]의 y축 속도
* 액터[자신]의 선 속도
* 액터[자신]의 각 속도

---

# 랜덤 {#random}
* 랜덤 정수 시작[0] ~ 끝[255]
* 랜덤 부동소수점  시작[0] ~ 끝[1]


---

# 터치 {#touch}
* [터치의 x좌표]
* [터치의 y좌표]
* [x좌표에서 터치 이동거리]
* [y좌표에서 터치 이동거리]
* 액터[자신]위의 터치갯수
* [화면을 터치하고 있다면]
* 액터[]를 터치하고 있다면
* 액터[]의 터치가 켜져 있다면

---

# 데이터 {#data}
* 리스트[]에서 마지막 원소 하나 빼오기
* 리스트[]의 원소 갯수
* 리스트[]의 인덱스[]의 값
* 리스트[]의 모든 원소 나열, 구분자[,]
* 리스트[]에서 값[]의 인덱스
* 리스트[]이 있다면
* 사전[]에서 키[]에 대한 값
* 키[]에 대한 값
* 키[]의 값이 있다면

---

# 문자열 {#string}
* 문자열 결합 [] + [], 구분자[:]
* 문자열[]이 접두사[]을 가진다면
* 문자열[]이 접미사[]을 가진다면
* 문자열[]에서 왼쪽에서 []번째 글자까지만 추출
* 문자열[]에서 오른쪽에서 []번째 글자까지만 추출
* 문자열[]에서 부분 문자열 추출 시작 인덱스[] 길이[]
* 문자열[]의 글자수
* 문자열[]이 문자열[]을 포함하는가?
* 문자열[]에서 인덱스[]의 글자 코드
* 글자 코드[]의 문자
* 키보드 글자
* 음성인식된 글자
* 번역된 글자
* 문자열[]이 숫자만 있다면
* 문자열[]을 대문자로 변환

---

# 날짜시간 {#datetime}
* 올해
* 이번 달
* 오늘 날짜
* 오늘 요일
* 현재 시간
* 현재 분
* 현재 초
* 타임스탬프

---

# 기타 {#etc}
* 스톱워치[]의 시간(소수점[]자리까지)
* 키[]에 대한 이벤트 인수 값
* 경로[]에 파일이 존재한다면
* 현재 언어

---

# 하드웨어 {#hardware}
* 묵음상태라면
* 배터리 충전퍼센트(0~100)


---



